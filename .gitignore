__pycache__/
*.pyc

# Compiled deps (use install script or Docker image; in Docker deps live at /dependencies)
src/dependencies/

# Secrets (do not commit)
hftoken_write.txt

# LAZ data (downloaded from Hugging Face, not stored in repo)
*.laz
data/toy_laz_dataset/raw/
